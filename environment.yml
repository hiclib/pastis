name: pastis
channels:
    - default
dependencies:
    - python
    - setuptools>=77
    - numpy
    - pip
    - pip:
        - sphinx
        - scipy
        - scikit-learn
        - matplotlib
        - git+https://github.com/gesinecauer/iced.git@fix_install_from_repo
        - joblib
        - pandas
        - sphinx-gallery
        - numpydoc
        - pytest
        - nose
        - autograd
